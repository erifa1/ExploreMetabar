% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color_helpers.R
\name{apply_color_csv}
\alias{apply_color_csv}
\title{Apply a parsed color CSV against the ground-truth modality list.}
\usage{
apply_color_csv(df, modalities_list, accept_numeric = TRUE)
}
\arguments{
\item{df}{data.frame returned by [read_color_csv()].}

\item{modalities_list}{ground-truth named list (var -> character vector
of valid modalities).}

\item{accept_numeric}{whether to honor `__numeric__` rows. Set to `FALSE`
when parsing the taxa CSV.}
}
\value{
list with elements:
  * `colors` — named list of named hex vectors (factors only).
  * `numerics` — named list of `package::palette` strings.
  * `dropped` — data.frame of dropped rows with a `reason` column.
}
\description{
Validates each row and silently drops rows that don't match the
ground-truth (with a structured report so the UI can surface what was
rejected). Rows whose `variable == "__numeric__"` are routed to the
numeric palette map; `modality` then carries the numeric variable name.
}
\keyword{internal}
